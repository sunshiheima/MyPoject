大家现阶段还是更喜欢axios多一些，因为fetch还有很多别的注意事项。

由于fetch是一个非常底层的API，它并没有被进行很多封装，还有很多问题需要处理：
1.不能直接传递JavaSript对象作为参数
2.需要自己判断返回值类型，并执行响应获取返回值的方法
3.获取返回值方法只能调用一次，不能多次调用
4.无法正常的捕获异常
5.老版本浏览器不会默认携带cookie
6.不支持jsonp

